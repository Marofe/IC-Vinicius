function h = media_nula_h(W, H, alpha, L)
%% MEDIA_NULA_H (Legacy Portuguese Alias -> frechet_mean_h)
% Retained for backwards compatibility with existing MEX and scripts.
h = frechet_mean_h(W, H, alpha, L);
end